{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Lighthouse",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/lighthouse/main.go",  // your main package
      "cwd": "${workspaceFolder}",                          // 👈 force working dir = repo root
      "console": "externalTerminal",   // 👈 use terminal for stdin/stdout
      "env": {
        "ENVIRONMENT": "dev"
      }
    }
  ]
}
